# .env file for local development
# Copy this to .env in your project root and customize as needed
# Note: Add .env to .gitignore to avoid committing sensitive data

# Database Configuration (matches Kubernetes ConfigMap)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=k8s_assignment
DB_USER=postgres
DB_PASSWORD=your_local_dev_password_here

# ASP.NET Core Configuration
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001
